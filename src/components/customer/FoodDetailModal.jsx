import { Minus, Plus, X } from 'lucide-react'

export default function FoodDetailModal({ item, quantity, onQuantity, onAdd, onClose }) {
  if (!item) return null
  return <div className="customer-modal-backdrop" onClick={onClose}><section className="food-detail-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close food details"><X size={18} /></button><img src={item.image} alt={item.name} /><div className="food-detail-body"><span className="dish-tag">{item.tag}</span><h2>{item.name}</h2><p>{item.description || (item.comboOffer?'A complete combo prepared together for your table.':`A carefully prepared ${item.category.toLowerCase()} favourite, made fresh for your table.`)}</p>{item.comboOffer&&<div className="combo-detail-items">{item.comboItems.map(entry=><span key={entry.id}><b>{entry.name}</b><small>BDT {entry.price}</small></span>)}</div>}<strong>BDT {item.price} {item.comboOffer&&<del>BDT {item.regularPrice}</del>}</strong><div className="detail-controls"><div className="quantity-control"><button onClick={() => onQuantity(Math.max(1, quantity - 1))} aria-label="Decrease quantity"><Minus size={14} /></button><b>{quantity}</b><button onClick={() => onQuantity(quantity + 1)} aria-label="Increase quantity"><Plus size={14} /></button></div><button className="detail-add-button" onClick={() => onAdd(item, quantity)}>{item.comboOffer?'Add combo to cart':'Add to cart'} <Plus size={15} /></button></div></div></section></div>
}
