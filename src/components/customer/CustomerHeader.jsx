import { Bell, QrCode, ShoppingBag, Store } from 'lucide-react'

export default function CustomerHeader({ restaurantName='Restaurant', logoUrl='', cartCount, setRole, onCart, onMenu, tableNumber='12', outlet='Dhanmondi', showAdmin=false }) {
  return <header className="customer-top"><a className="customer-live-brand" href="#customer"><img src={logoUrl||'/favicon.svg'} onError={event=>{event.currentTarget.src='/favicon.svg'}} alt=""/><b>{restaurantName}</b></a><div className="table-badge"><QrCode size={15}/> Table {tableNumber} <span>•</span> {outlet}</div><div className="customer-actions">{showAdmin&&<button className="portal-back" onClick={()=>setRole('owner')}><Store size={15}/> Admin</button>}<a className="customer-offers-link" href="#offers">Offers</a><button className="customer-order-now" onClick={onMenu}>Order now</button><button className="customer-icon-action" onClick={onCart} aria-label={`Open cart with ${cartCount} items`}><ShoppingBag size={19}/>{cartCount>0&&<strong>{cartCount}</strong>}</button><button className="customer-icon-action" aria-label="Customer notifications"><Bell size={18}/></button></div></header>
}
